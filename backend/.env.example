# Example DATABASE_URL values
# Postgres (recommended):
# DATABASE_URL="postgresql://user:password@localhost:5432/expense_pro"

# SQLite quick dev (use for local tests):
DATABASE_URL="file:./dev.db"

# JWT secret
JWT_SECRET="replace-with-a-secret"
